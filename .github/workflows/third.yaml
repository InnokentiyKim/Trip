name: third-workflow
on:
  issues:
    types: [opened, deleted]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event vars
        run: |
          echo $GITHUB_EVENT_NAME
          echo $GITHUB_SHA
          echo $GITHUB_REF
