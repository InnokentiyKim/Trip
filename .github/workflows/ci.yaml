name: First-workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: list-files-and-copy-repo
        run: ls -la
      - uses: actions/checkout@v4
      - name: list files
        run: ls -la